import { StudentdatatransformationPipe } from './studentdatatransformation.pipe';

describe('StudentdatatransformationPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentdatatransformationPipe();
    expect(pipe).toBeTruthy();
  });
});
